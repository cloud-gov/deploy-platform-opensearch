---
platform: linux

inputs:
  - { name: deploy-logs-platform-config }


run:
  path: deploy-logs-platform-config/ci/reset-aide.sh

params:
  BOSH_CA_CERT:
  BOSH_ENVIRONMENT:
  BOSH_CLIENT:
  BOSH_CLIENT_SECRET:
  BOSH_DEPLOYMENT_NAME:
  BOSH_INSTANCE_NAME:
