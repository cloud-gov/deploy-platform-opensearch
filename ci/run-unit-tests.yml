platform: linux

inputs:
- name: deploy-logs-platform-config

run:
  path: sh
  args:
  - -c
  - |
   cd deploy-logs-platform-config/
   python -m pip install -r requirements-test.txt
   python -m pytest tests
